model User {
  id        String    @id @default(uuid())
  name      String?
  email     String    @unique
  password  String
  createdAt DateTime  @default(now())
  news      News[]
  comments  Comment[]

  @@map("users")
}
